# Generated by Backstage scaffolder — do not edit manually.
# Calls the central reusable CI/CD workflow in nsudhakar-devcloud/platform-svc-mvp.
name: CI-CD

on:
  push:
    tags:
      - 'v*-rc*'
    branches:
      - main
      - development
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  cicd:
    uses: nsudhakar-devcloud/platform-svc-mvp/.github/workflows/cicd.yaml@main
    with:
      project_name: "headlamp"
      product: "nrp"
      dockerfile_name: "Dockerfile"
      semver: ""
    secrets: inherit
